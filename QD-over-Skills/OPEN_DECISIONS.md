# OPEN DECISIONS — 需要公司决策的阻塞项

> **pull 本项目后先读这里。** 本文件列出**当前阻塞、需公司侧拍板/提供**的事项。已解决的请就地填答案并标 ✅；新增阻塞项追加到文末。

---

## 🚩 BLOCKER-1：API / 模型访问

**现状**：无 GPT-5.5 访问。已用 DeepSeek（`patches/deepseek-backend-adapter.patch`）在 SearchQA 上把 SkillOpt 复现端到端跑通（见 `baseline_report.md`、`results/`）。**接下来的 QD 实验需要公司确认下列各项。**

### 需要公司回答 / 提供

1. **我们到底有哪些可用 API？**
   - [ ] 仅 DeepSeek（滚动版）
   - [ ] 前沿厂商（OpenAI / Anthropic 等），且**带日期快照**
   - [ ] API 托管的**固定开源权重**模型（如某 dated 的 Qwen / Llama）
   - 答：__________________________________________

2. **是否有「可钉死版本 / 带日期快照」的模型？**
   - *为什么重要*：headline 结果必须可复现；DeepSeek 滚动版会随时间漂移，跨时间窗重跑无法保证「frozen target」诚实。
   - 答：__________________________________________

3. **预算 / 速率限制？**（此前提到「不限量」，请确认是否属实，以及有无 RPM / TPM 上限。）
   - *为什么重要*：QD 的昂贵评估按「不同打法数」成倍增加，需据此定档案规模与并行度。
   - 答：__________________________________________

4. **能否用一个更强的模型当 optimizer、便宜的钉死模型当 target？**
   - *为什么重要*：**optimizer 的强弱直接决定本项目最大的技术赌注能否成立**——「瞄准着采」能不能产出行为上**真不同**的候选（SPEC 命题 3.8 依赖 i）。更强 optimizer 直接给这个命门减风险，而它只增加训练期成本、不增加部署成本。
   - 答：__________________________________________

5. **agent / CI 能否拿到 API key 自行运行昂贵实验？** 还是由公司按脚本运行？
   - *为什么重要*：决定昂贵实验做成「谁按都行的一键脚本」后交给谁跑。
   - 答：__________________________________________

### 在 BLOCKER-1 未解决前

- **可继续（不花钱、本地）**：读 `SkillOpt/` 源码坐实实现、搭 `qd/` 模块骨架、实现 `V/b/U/Π/loop`、**T001（K=1 回归测试）**、廉价 smoke test。
- **被阻塞（需先定 API）**：Phase-1 楔子实验（T008）、headline 对比（T011）等一切**昂贵付费**步骤。

---

_其它阻塞项后续追加于此。_

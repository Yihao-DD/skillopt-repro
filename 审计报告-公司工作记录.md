# 审计报告：公司 SkillOpt / QD-over-Skills 工作记录

> **审计对象**：`工作记录-完整版.md`（公司侧 2026-06-08 工作记录，跑在内部 venus.oa.com llmproxy / `qwen3-235b-a22b-2507-fp8`）
> **审计日期**：2026-06-08
> **方法**：9 路并行子代理，逐条把记录中的每个声明核对 against 本地实际源码（`SkillOpt/` fork、`qd/`、`QD-over-Skills/` SPEC + ADR）＋一轮对抗式完备性复核。所有判定均带 `file:line` 证据。
> **分级**：✅ 有效 ｜ ❌ 无效 / 危险 ｜ 🔴 根本问题（公司与原团队都未注意）
> **范围说明**：公司从**上游** `microsoft/SkillOpt`（commit `b5328e8`）起步，自写了一套 `qd/`（`qd/loop.py`、文字长度 descriptor），该代码**不在本地仓库**，故对其自身代码的判定基于记录文本描述；对上游 / SPEC / 我们仓库内 `qd/` 的判定基于源码核实。

---

## 0. 总判断（TL;DR）

公司做了扎实的工程：vanilla SkillOpt 复现正确、管线能端到端跑、并定位到了 cosine 调度这一真实机制。**但最关键的事实是——这次跑的「full QD」实际上根本不是 QD。**

他们的行为描述子用了 **skill 文档长度**（违反「只从轨迹算」红线的文字特征）＋ **验证集命中率**（fitness 特征），把所有候选都塌进了**同一个格**（`n_occupied=1`，跨格 pickup=0）。由命题 3.6，这在定义上等同于 **vanilla K=1 贪心** —— QD 的探索机制（空格直接收）**从未触发**。

**因此**：他们对 0/8 全 reject 的整个诊断（edit_budget、gate 太严）都在**治标**，真正的 bug 在更上游的 descriptor；他们 §4 那张「QD vs vanilla」对照表，其实是 **vanilla 对 vanilla**。此外，凡涉及 headline 数字的部分，都被一个**没固定版本、没 seed、temperature=0.7 的后端**和**从未做过的统计检验**所污染——`+0.0893` 这个增益就坐在 `±0.06` 的测量噪声里。

**净结论**：可保留的只有 2.5 项；2 项补救方案是红线违规 / 引入新混淆；另有 11 个公司与原团队都未注意、其中 4 个 Critical 的根本问题。**在重建 descriptor、冻结 target、做统计检验之前，目前没有任何一个 QD-vs-vanilla 结论是可信的。**

---

## 1. 结论速览

| # | 项 | 判定 | 严重度 |
|---|---|---|---|
| F1 | OpenAI-compat 参数修复（`max_tokens` / 去 `reasoning_effort`） | ✅ 有效，但与我方补丁**重复** | Medium |
| F2 | cosine 让 vanilla 末段 edit_budget 缩到 2 | ✅ vanilla 侧成立；用它解释 QD 0/8 **未证实/可能为假** | High |
| F3 | K=1 回归测试盖不住「候选生成」路径 | ✅ 有效发现 | High |
| R1 | Plan B：ε-软化 gate（`cand ≥ parent − 0.02`） | ❌ **红线违规**，必须删 | **Critical** |
| R2 | §3.10「档案更新 ≡ heavy-ball 动量」+「0/8 不影响理论」 | ❌ 过度声称，降级为类比 | High |
| R3 | Plan A：重跑钉死 `edit_budget=2` | ❌ 引入**新**混淆 | High |
| P1 | **descriptor 红线违规 + 档案塌缩 → 这次根本没在做 QD** | 🔴 根因 | **Critical** |
| P2 | frozen-target 红线实践中已违反（无 seed / temp 0.7 / 不钉版本） | 🔴 | **Critical** |
| P3 | 统计显著性从未做，数字小到不显著 | 🔴 | **Critical** |
| P4 | 「同等评估预算」硬判据从未执行；效率机制未跑 | 🔴 | **Critical** |
| P5 | 比较未受控：两边 baseline 各自重采样、起点不同 | 🔴 | High |
| P6 | 两套互不兼容 qd/，跑的是不合规的那套（推翻 ADR-0002） | 🔴 | High |
| P7 | 未提交/未追踪 git 状态 → headline 从源码不可复现 | 🔴 | High |
| P8 | 论文在任何有效结果之前就写好了 | 🔴 | High |
| P9 | 运维脆弱：`pkill` 误杀 2/3 跑、smoke+full 共用代理 | 🔴 | Medium |
| P10 | 1 步 smoke 啥关键路径都没验证 | 🔴 | Medium |
| P11 | 成本无界，BLOCKER-1 Q3（预算/限流）仍空白 | 🔴 | Medium |

---

## 2. ✅ 有效、值得保留

### F1 — OpenAI-compat 参数修复〔有效，但是重复〕
- **是什么**：`azure_openai.py` 新增 `_is_openai_compat_client()`，对 `openai_compatible` 客户端用 `max_tokens`（非 `max_completion_tokens`）、且不传 `reasoning_effort`。
- **核实**：技术上正确且必要——venus/qwen3-235b 这类严格 OpenAI-compat 端点确实对 `max_completion_tokens`/`reasoning_effort` 报 400。用 client 类型（`OpenAI` vs `AzureOpenAI` 子类）判断而非猜模型名，稳健；两个 chat 入口都改了。
- **⚠️ 但**：这与我方已有的 `patches/deepseek-backend-adapter.patch` **逐字节相同**——他们在上游重新推导了一遍我们早有的补丁。这是**重叠/重复**，不是新贡献，**合并时要对账，避免双重 apply / 冲突**。
- 证据：`azure_openai.py:354-361, 412-426, 503-519, 295-300`；`patches/deepseek-backend-adapter.patch:1-70`。

### F2 — cosine 让 vanilla 末段 edit_budget 缩到 2〔vanilla 侧成立；QD 侧诊断未证实〕
- **vanilla 机制为真，已代码核实**：`CosineScheduler._compute_lr`（`scheduler.py:77-82`）在 `max=4,min=2,total_steps=8` 下给出 `[4,4,3,3,3,2,2,2]`，step 7 = 2；`rank_and_select`（`clip.py:54`）以 `edit_budget` 为保留 edit 数上限；故 vanilla 唯一一次 accept（step 7）是用 2 个小 edit 把 sel 从 0.55→0.625 过的严格 gate。SpreadsheetBench config 继承 `_base_` 的 cosine、未覆盖（`configs/spreadsheetbench/default.yaml:1-16`）。
- **❗ 承重半句未证实，且可能为假**：诊断的另一半「我们 QD 全程固定 `edit_budget=4`」**从未核实**——`qd/loop.py` 不在仓库，而记录**自相矛盾**：`195` 行说 QD STEP 1 调了 `build_scheduler`（**默认就是 cosine**！），`291` 行又把「确认 cosine 每步实际 budget」列为**待办**。若 QD 也走了 cosine，则 step 6-8 同样缩到 2，「4 vs 2 的差异」在 vanilla 拿到唯一 accept 的那个窗口**根本不存在**。记录从未记录任一步 QD 的 `n_edits_ranked`。
- **行动**：重跑前先 **grep QD 跑的 `history.json` 每步 `edit_budget`**，30 秒即可证伪/证实，省掉 ~3h 无效重跑。
- 证据：`scheduler.py:74-82`；`configs/_base_/default.yaml:74-77`；`clip.py:52-108`；`trainer.py:1174-1181, 741-746`；记录 `195/285/291`。

### F3 — K=1 回归测试盖不住「候选生成」路径〔有效发现〕
- **核实**：两个 K=1 测试只 import `evaluate_gate` / `apply_patch` / `Archive`，喂的是**写死的 `(skill, score)` 序列**；`Archive.update`（`archive.py:56-73`）消费**预先算好的** `cand_score`，是纯严格 `>` 比较器，**零生成逻辑**。`ADR-0001:44-47` 自己也把 T001 范围限定在「step 选择逻辑」、明确不复现生成。
- **含义**：「K=1 测试全绿」只证明了 accept/reject **判定边界**与 SkillOpt 一致，**不能**证明整条循环（含 cosine 调度 + `rank_and_select` 的候选生成）与 vanilla 一致——这正是 full 跑偏离 vanilla 的盲区所在。「测了比较器，没测候选」。
- 证据：`test_k1_reduces_to_skillopt.py:15-17,50-99`；`test_k1_characterization.py:18-25`；`qd/archive.py:56-73`；`ADR-0001:44-47`。

---

## 3. ❌ 无效 / 危险 / 必须否决

### R1 — Plan B：ε-软化 gate（`cand ≥ parent − 0.02`）〔🔴 红线违规·Critical〕
- 接受**比 parent 还差**（最多差 0.02）的候选——正是 SPEC「−22.5 消融」惩罚的「格内下降漂移」，BRIEF §4 红线第 3 条明文禁止（「永不接受格内下降，平局拒，**非协商**」）。
- 它会**证伪命题 3.4 的单调性**、**毁掉论文核心卖点「不松 gate 的探索」**、**破坏 K=1≡SkillOpt 回归**。
- **名字误导**：SPEC 的「信任域」（命题 3.7）约束的是**编辑步长 `ρ ≤ L_t`**，不是分数容差——这个名字把一个被禁的 fitness 下降机制包装成了已满足的无关机制，极易在评审中蒙混。
- **判定**：不是有效修复，是「用降低门槛来制造 accept」。**直接从补救清单删除。**
- 证据：`BRIEF.md:42`；`方案与数学推导.md:88-100, 110-112, 168`；`gate.py:123-124`（上游严格 `>`，已核实）；`ADR-0001:18-21,44-45`；记录 `293,367`。

### R2 — §3.10「档案更新 ≡ heavy-ball 动量」+「0/8 不影响理论」〔过度声称·High〕
- 档案更新是**逐格取 max**（幂等次序统计量，无衰减系数），**不是** heavy-ball 的线性指数平均；无法在不加额外（未声明）结构的前提下读出 `μ̄∈[0,1)`。
- MAP-Elites 各格**不可比**；真正贡献（命题 3.8 跨格逃逸）恰是单条动量递推**表达不了**的。把 K 个不可比格塌成 `E*_t=max_j E_t(j)` 的单一递推，**正好丢掉了逃逸机制**。
- `μ̄=0 ⇔ K=1` 是**空话**（命题 3.6 早已不靠动量给出 K=1 归约）；唯一「精确」的区域恰是**没有动量**的区域。
- 「纯理论不依赖实验」只对**平凡的单调性引理**成立；本次跑实现的是 `μ̄=0 ⇔ K=1` 退化极限（1 格），**对它要论证的 K>1 理论提供零经验支持**。
- **行动**：§3.10 从「等价」**降级为明确标注的「类比」**；真要谈动量，应锚在**慢更新（slow-update）**那条真实的二时间尺度机制上（`研究框架.md:18` 原本就是这么类比的）。非红线违规，但属评审可直接拒的过度声称。
- 证据：`方案与数学推导.md:84-99, 114-121, 105`；`研究框架.md:18`；记录 `278-282, 312-313`。

### R3 — Plan A：重跑钉死 `edit_budget=2`〔引入新混淆·High〕
- vanilla **不是**固定 2，是 cosine（4→2）。把 QD 全程钉 2，会让 QD 在 epoch 1-3 步子**比 vanilla 还小**——又是一个 budget 假象，又一个不可解释的结果，白烧 ~3h。
- **正确做法**：让 QD 用与 vanilla **完全相同的 cosine 调度**（`build_scheduler`, `cosine, max=4, min=2, total_steps=8`），两臂都 log 每步 budget 并断言相等，使**唯一有意预期的差异是档案结构（K=1 vs K=16）**。
- 证据：记录 `293,366`；`scheduler.py:74-82`。

---

## 4. 🔴 根本问题：公司与原团队都没注意到的（按重要性）

### P1〔根因·Critical〕这次「full QD」根本没在做 QD
公司 descriptor = **b₁=skill 文档长度归一化** + **b₂=验证集命中率**：
- **b₁ 是 skill 文字特征，直接违反红线**（`BRIEF.md:43` / `方案与数学推导.md:62-68` Def 3.1：「descriptor 只从轨迹 τ 算，绝不从 skill 文字算，否则 template collapse」）。
- **b₂ 是 fitness 特征**，`ADR-0002:11` 已警告会「退化成 fitness」，所有候选饱和在 ≈0.97-1.0。
- 结果：baseline 与全部 6 个候选都落进 **cell 7**；`n_occupied=1`、跨格 pickup=0、档案 `{cell 7: step 0}` 从未变过。**唯一探索通道（空格直接收，Def 3.3）从未触发**；由命题 3.6，这就是逐字定义上的 **vanilla K=1 贪心**。
- **含义**：0/8 这个结果**不含任何关于 QD 的信息**；§5.3/§9 的补救（降 budget、软 gate）**改变不了 `n_occupied`**，因为 bug 在 descriptor。我们仓库内 `qd/descriptor.py:58-93`（从生成代码结构取轴，合规、带文字无关性测试）本就是对的——公司没用它，另写了违规版。
- **修复**：改用轨迹轴重建 descriptor，并把 b₂ 换成**非饱和**行为轴；`n_occupied>1` 复现前，不得做任何 QD 结论。
- 证据：记录 `58-59,188-192,224,236,280-281`；`BRIEF.md:43`；`方案与数学推导.md:62-68,88-95,105`；`ADR-0002:8,11`；`qd/descriptor.py:58-93`；`qd/tests/test_descriptor_validation.py:4-14`。

### P2〔Critical〕frozen-target 红线在实践中已被违反 —— headline 落在噪声里
- 活跃的 `azure_openai.py` 调模型时**既无 `temperature=0` 也无 `seed`**（`411-429, 502-522`）；qwen 路径 temperature 默认 **0.7**、亦无 seed（`qwen_backend.py:67,189-196`）。他们设的 `seed=42` **只是数据采样种子，传不到模型**（`trainer.py:1322-1331`）——**虚假的可复现感**。
- baseline_test 三次 = **0.4357 / 0.5000 / 0.4500**（±0.06）。他们归因「后端漂移」，但**主因是自己没固定的 temp-0.7 解码**（易修），fp8 共享代理只是次因。
- headline **+0.0893** 就坐在 **±0.06 噪声**里；那唯一一次 accept 还是两个独立采样噪声分数（0.625 vs 0.55）做严格 `>` 的结果。**目前不可信。**
- SPEC §1/§5/§7（`方案与数学推导.md:17,179,193-195`）把「钉死带日期快照的 frozen target」列为不可谈判；`OPEN_DECISIONS.md:19-21` 的 BLOCKER-1 Q2 正是此条。**红线已违反，且未做任何缓解**（§5/§9 全在调 edit_budget/gate，零 determinism 处理）。
- **修复**：钉死模型快照 + `temperature=0` + 推理 seed；baseline 跑 3-5 次出 CI。
- 证据：`azure_openai.py:411-429,502-522`；`qwen_backend.py:67,189-196`；`trainer.py:1322-1331`；`方案与数学推导.md:17,179,193-195`；`OPEN_DECISIONS.md:19-21`；记录 `17-19,181,226`。

### P3〔Critical〕统计显著性从未做，数字小到不显著
- +0.0893 ≈ 280 题里 25 题；整个 vanilla 结果押在**一次 accept**，而那次只是 val（n=40）里 **3 道题**的摆动（0.55→0.625）。
- n=280 两比例检验差值 95%CI ≈ **±0.082**，效应勉强出噪声带；正确的**配对 McNemar**（同 280 题）依赖**从未报告的不一致对数**。
- 他们把 0.014 的差读成「同量级 ✅」，还在结果列填「**预测** 0.000」。**「+0.0893 的赢」与「0/8-vs-1/8 的输」在统计上都是掷硬币。**
- **修复**：同 280 题配对 McNemar + 报不一致对数 + Δ 的 CI；baseline/final 各跑 3-5 次出经验 CI；step-7 accept 复现前只当轶事；按 n=40/280 预登记最小可检测效应。
- 证据：记录 `17,167,225,251-252`；val n=40（1 题 = 0.025）。

### P4〔Critical〕「同等评估预算」硬判据从未执行；效率机制根本没跑
- SPEC 把「同预算下取胜」列为**不可谈判**（`方案与数学推导.md:178`；命题 3.9：失败则「headline 不可信」）。
- full 跑**没有任何** cheap_rollouts / cache_hits / probe 快筛 / 行为去重 / surrogate / Π 调度记录，token 也只统计了 vanilla。**让 QD 有资格被比较的机制三压根没接进 loop**。每个候选塌进一个格，命题 3.9 的节省因子 `N/min(N,K)` 也从未实现。
- **比 descriptor 塌缩更深**：即使修好 descriptor，这次比较在预算口径上也不成立。
- **修复**：两臂接一个**共享的「昂贵评估次数」计量器**并断言相等；把机制三接进 loop，在 `N'>1` 的跑上验证命题 3.9 的节省。
- 证据：`方案与数学推导.md:132-154,178,203`；`研究框架.md:73,133`；记录 `170,191`。

### P5〔High〕比较未受控：两边 baseline 各自重采样、起点不同
QD 起于 baseline_sel=0.50、vanilla 起于 0.55（test 0.45 vs 0.4357）。这是**同一初始 skill 经非确定后端各采一次**的噪声，却让严格 `>` gate 的**门槛不同**——「1 accept vs 0」部分只反映谁抽到更低方差的 baseline。他们却把 0.50 当成 QD 的「更难平台期」（方法属性）。**修复**：temp=0 下把 baseline 算一次、冻成常量、两臂共用。证据：记录 `246,247,287`。

### P6〔High〕两套互不兼容 qd/，跑的是不合规那套（推翻 ADR-0002）
公司在另一 clone 自写 qd/（loop.py + 文字长度 descriptor），与我们仓库 `ADR-0002`（已接受，代码级 φ）**语义分叉**且**悄悄推翻**之。所有 full-run 数据都是被本项目自己 ADR 否决的代码产出，且无 provenance。**修复**：指定唯一权威实现（仓库内合规版），其余删除/移植，全部重跑；每个 `summary.json` 盖 `qd/` git SHA + descriptor 版本。证据：记录 `13,53-64,218`；`qd/descriptor.py:58-93`；`ADR-0002`。

### P7〔High〕未提交/未追踪 git 状态 → headline 从源码不可复现
`azure_openai.py` 改了未提交；`qd_train.py`/`launch_qd.sh` 未追踪；`qd/`、`results/` 在任何 git 树之外；headline 跑从**脏工作树**启动，无 SHA/tag/lockfile。叠加非确定后端 —— **代码与推理都没钉住，双重不可复现**。**修复**：下次跑前全部提交入一个追踪仓库；每个 `summary.json` 盖 git SHA + config + pip-freeze。证据：记录 `344-358,36`。

### P8〔High〕论文在任何有效结果之前就写好了
454 行正稿（含 3.10/3.11/3.12、abstract、FIG-1~4）在 full 还在 STEP 6、final test「待跑」时就**用 ~8 分钟写完**，结果列填「预测」；§6 预写「即便 0/8 理论仍成立」给叙事上保险。**这制造强确认偏误**，也是 Plan B（唯一能「造」出 accept 的选项）变诱人的动机。**修复**：预测与实测分列；有效结果前不定稿；明列证伪条件（如「合规 descriptor 下若无跨格 pickup，则 QD 主张失败」）。证据：记录 `233-234,251-252,300-313,329`。

### P9-P11〔Medium〕
- **P9 运维脆弱**：`pkill qd_train.py` 一锅端**误杀 3 次 full 里的 2 次**；smoke 与 full **共用同一 fp8 代理**，给被测跑加并发噪声、并可能半写 `summary.json`。→ 按 PID 杀、串行化/分离端点、原子写（temp+rename）。证据：记录 `203-208,38`。
- **P10 1 步 smoke 啥也没验证**：只跑出一次 REJECT、一个格，**accept 分支与所有 QD 机制都没碰**，却被当成「全链路已验证」——正是 descriptor bug 漏进 3 小时付费跑的原因。→ 换成能**强制 ≥2 格 + 触发空格 accept** 的有意义集成测试。证据：记录 `182,188-192,197`。
- **P11 成本无界**：单臂 2h47m / 45.6M tokens；SPEC 矩阵（SkillOpt+EvoSkill+QD+≥3 消融 ×2 benchmark ×3-5 重复）是几十个这种跑，**无成本模型，BLOCKER-1 Q3 仍空白**。→ 立刻建成本模型并提交 BLOCKER-1 Q3。证据：记录 `138-139,170`；`方案与数学推导.md:174-178`；`OPEN_DECISIONS.md:23-25`。

---

## 5. 建议的行动顺序

1. **立刻删掉 Plan B**（红线违规）。
2. **先花 30 秒证伪核心诊断**：grep QD 跑 `history.json` 每步 `edit_budget`；若也是 4→2，「budget 错位」假说当场作废，省 ~3h。
3. **重建 descriptor 用轨迹轴**（直接用仓库内合规 `qd/descriptor.py`），b₂ 换非饱和行为轴。**根因修复——不修，任何 QD 结果都是伪装的 vanilla。**
4. **冻结 target**：钉死带日期快照 + `temperature=0` + 推理 seed；baseline 跑 3-5 次出 CI（顺带回答 BLOCKER-1 Q2）。
5. **baseline 算一次、冻成常量**，两臂共用，消除每跑重采样混淆。
6. **跑受控 A/B**：两臂用相同 cosine 调度（4→2，非固定 2）+ 共享昂贵评估计量器断言相等 + 机制三真正接进 loop；**要求 `n_occupied>1` 才谈 QD 效果**。
7. **每个对比加统计**：同 280 题配对 McNemar + 不一致对数 + Δ 的 CI；step-7 accept 复现前只当轶事。
8. **代码归一 + 入库**：指定权威 qd/，提交所有未提交代码，每个 `summary.json` 盖 git SHA + config + pip-freeze。
9. **论文 §3.10 降级为「类比」**，动量锚到慢更新；有效结果前不定稿，明列证伪条件。
10. **运维加固**：按 PID 杀、分离/串行端点、原子写、QD-meaningful smoke；并对账 deepseek-compat 重复补丁。
11. **建成本模型**（tokens/run × 臂 × benchmark × 重复）提交 BLOCKER-1 Q3，再花钱。

---

## 附录 A：逐条声明核验摘要

| 声明 | 判定 | 红线？ | 关键证据 |
|---|---|---|---|
| cosine→vanilla budget 4→2，step7 用 2 | partially-valid | 否 | `scheduler.py:74-82`；`clip.py:52-108`；`trainer.py:1174-1181`；记录 195/285/291 自相矛盾 |
| openai-compat（max_tokens/去 reasoning_effort） | valid（重复） | 否 | `azure_openai.py:354-361,412-426`；`deepseek-backend-adapter.patch:1-70` |
| Plan B 软化 gate | valid（确认违规） | **是** | `BRIEF.md:42`；`方案与数学推导.md:110-112`；`gate.py:123` |
| descriptor=（文档长度, 命中率）→塌缩 | valid | **是** | `BRIEF.md:43`；`方案与数学推导.md:62-68`；`ADR-0002:8,11`；记录 188-192/224/236/280 |
| K=1 测试只覆盖判定边界 | valid | 否 | `test_k1_reduces_to_skillopt.py:15-17`；`archive.py:56-73`；`ADR-0001:44-47` |
| baseline 漂移 0.4357/0.50/0.45 | valid | **是** | `azure_openai.py:411-429`（无 temp/seed）；`trainer.py:1322-1331`（seed 仅数据） |
| §3.10 动量等价 + 0/8 理论仍成立 | partially-valid | 否 | `方案与数学推导.md:84-99,114-121,105`；`研究框架.md:18` |

> 本报告所有判定来自对本地源码的逐条核对；公司自写 `qd/`（`loop.py`、文字长度 descriptor、`qd_train.py`、`launch_qd.sh`）不在本地仓库，相关判定基于记录文本，已在文中标注「未证实」处。

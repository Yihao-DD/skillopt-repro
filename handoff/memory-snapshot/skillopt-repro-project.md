---
name: skillopt-repro-project
description: The E:\skillopt project — research extension of microsoft/SkillOpt (Quality-Diversity); authoritative plan = the QD-over-Skills package. As of 2026-06-07 the SpreadsheetBench baseline + T001/T002 + Phase-1 QD components are done; critical path now blocked on BLOCKER-1 (company API/budget).
metadata: 
  node_type: memory
  type: project
  originSessionId: ff54177a-3262-4e53-b9cb-bc92bf633e25
---

`E:\skillopt` extends **microsoft/SkillOpt** (text-space skill optimizer for frozen LLM agents; MIT) to **beat its benchmarks** via Quality-Diversity (MAP-Elites) search over skill docs.

**AUTHORITY = the `E:\skillopt\QD-over-Skills\` package** (package wins on conflict; `.tasks/INDEX.md` = SSOT; formal SPEC = `方案与数学推导.md` v0.2; root scaffolding is legacy). Method: replace SkillOpt's single greedy current-skill + global strict gate with a **MAP-Elites archive** binned by a **behavior descriptor from trajectory τ (never skill text)**; per-cell strict `>` gate (empty cell auto-accepts = exploration). Red lines: pure-API/no-GPU; **K=1 ≡ SkillOpt**; equal expensive-eval budget. Win: C0 K=1==SkillOpt / C1 escape on a wedge / C2 equal-budget beat SkillOpt+EvoSkill / C3 transfer.

**Contributors / compute:** worked by both Claude (local, Windows, venv py3.13) and **"Codex" on AutoDL** (remote, py3.12.3, runs the paid DeepSeek experiments). Workspace `E:\skillopt` → private GitHub **Yihao-DD/skillopt-repro**; **`master` cleaned + pushed `a975299` 2026-06-08 = canonical clean state** (root README rewritten as full code-map + smoke-reproduce per company request). SkillOpt fork at `E:\skillopt\SkillOpt\` (own git, gitignored); the openai-compat fix now lives ONLY in the fork working tree — `patches/deepseek-backend-adapter.patch` was DELETED (recoverable via archive tag) and must be committed into the fork at S11/S13. `.env` (DeepSeek key) gitignored — only `.env.example` tracked.

**STATUS (2026-06-07, board):**
- **T000 DONE** — vanilla SkillOpt reproduced on **both** benchmarks via DeepSeek (no GPT-5.5): SearchQA EM 0.747→0.804 (+5.6); **SpreadsheetBench (main battleground) EM 0.4571→0.5286 (+7.1)**, gain mostly cell-level. **Plateau reproduced** (8 steps = 2 accept/6 reject, best fixed at step 6, epochs 2&4 all-reject) = the greedy local-optimum QD must break. ~110M tok / ~$16-19 / ~117min. Artifacts: `results/ssb_dpsk_run1/` + `ssb_baseline_report.md`.
- **T001 DONE** — K=1 regression, 5 tests (gate-oracle equivalence ×2 + slow-update protected-field ×2 + real 8-step ssb history replay). Semantics in `decisions/ADR-0001`.
- **T002 DONE** — `qd/descriptor.py` code-level behavior descriptor (φ→Tier-A g→cell, not text), 8 tests, axes in `ADR-0002`.
- **T004–T007 REVIEW** — Phase-1 QD components built + smoke-green: `qd/variation.py` (aimed sampling V), `qd/archive.py` (K>1 archive + per-cell gate U), `qd/budget.py` (dedup/cache/cost Π1), `qd/scheduler.py` (adaptive-k/UCB Π2).
- **T003 BLOCKED, T008/T011 gated on BLOCKER-1.**

**BLOCKER-1 (open, company; THE critical path):** which APIs / dated-snapshot models / budget+rate limits / **can a stronger model be the optimizer** (de-risks the core bet: aimed sampling producing behaviorally-distinct candidates, Prop 3.8 dep-i) / who runs paid experiments. Gates T003 (wedge on SpreadsheetBench), T008 (Phase-1 integration = equal-budget escape), T011 (headline vs SkillOpt+EvoSkill). See `OPEN_DECISIONS.md`.

**Local/free work still open:** review T004–T007 (REVIEW→DONE), build the integration `loop.py` harness + loop smoke, T009 descriptor validation (text-vs-behavior). Tools: `tools/materialize_spreadsheetbench.py` (HF KAKA22/SpreadsheetBench → 80/40/280 split). Env gotcha: [[skillopt-dir-write-permission]].

**REORG + AUDIT (2026-06-08):** An audit (`审计报告-公司工作记录.md`, 14 failure modes) found the company ran a divergent local `qd/` with a red-line-violating descriptor (doc-length axis → archive collapsed to 1 cell → "full QD" was vanilla-vs-vanilla), dirty-tree headlines, temp=0.7/no-seed target. Response = 5 ADRs + `PROCESS.md` (the collaboration SSOT). Legacy cleanup: **per user 2026-06-08 the old cruft was DELETED, NOT sealed** — audit (`审计报告-公司工作记录.md`), work-record, old baseline reports, `docs/superpowers/`, `results/`, `patches/` removed from master; **all recoverable via pushed tag `archive/pre-reorg-2026-06-08`**; audit conclusions preserved in `ADR-0003`. **asymmetric one-way channels** (code: us→company via immutable tag; results: company→us via hand-delivered FEEDBACK zip + `ingest_feedback.py`); company = pure executor (run-only, never edits/commits — `在公司，` prefix = company mode). New surfaces to build: `scripts/` (preflight_gate, redline_lint, stamp_provenance, run_experiment, ingest_feedback, make_handoff), `configs/frozen/<model>@<date>.yaml` (temp=0+seed-to-model), `data/benchmarks/` (commit the ~15MB SpreadsheetBench tarball per ADR-0005), `runs/index.csv`. **ADR-0004**: vendor the SkillOpt fork into `vendor/SkillOpt/` (plain files, NOT submodule); commit the uncommitted openai-compat fix (`SkillOpt/skillopt/model/azure_openai.py` is dirty) as a fork commit, reconcile vs `patches/deepseek-backend-adapter.patch` (keep one). The preflight gate (`scripts/preflight_gate.py`) is the fail-closed checkpoint between "we finish coding" and "company spends money": asserts C0 K=1==SkillOpt, descriptor non-degeneracy, n_occupied>1+cross-cell pickup, equal-budget (one shared EvalCounter), frozen-target determinism double-run, shared-baseline, clean-tree+whole-tree-hash, provenance. **Critique sequencing rule: build qd/loop.py + tests + descriptor rebuild FIRST, write the gate LAST so it binds to real symbols and fails-closed on anything missing.**

**REORG EXECUTION (2026-06-08, branch `reorg/2026-06-08` == master):** done = S0 (branch + F1: patch already-applied, `git apply` fails) / F0 (`conftest.py` decouples import from the editable `.pth`; `REORG_PLAN.md` is the resume anchor) / S1 (6 MED/LOW qd/ bugs) / **S2+S3 (descriptor axis1 rebuilt: saturating 1-uses_pandas → graded op_density `n_ops/lines`, axis0=(code_len+ctrl)/2, uses_pandas kept in φ unused; ADR-0006 supersedes ADR-0002; test-first RED→GREEN, 29 passed; master `2605bca`)** / **S4–S9 (`qd/loop.py`: `produce_and_score_candidate`/`run_search`; inject `CandidateProducer` = zero-API; one shared `EvalCounter` = equal expensive-eval budget; K=1 decision == `evaluate_gate` closes F3; K>1 = descriptor routing + `deduplicate_by_behavior` + UCB `choose_parent_cell` + cross-cell pickup; 38 passed; master `1b8c07c`)** / S12. **Code-vs-audit: P1 + P4 (equal-budget) + P5 (shared-baseline) + F3 all addressed in code+tests (zero-API); still pending = a real SkillOpt model adapter (wire `CandidateProducer` to rollout/reflect/apply/eval) + the preflight gate (~S9 of S18); the real `n_occupied>1` on DeepSeek data is still unproven via the full loop (needs the adapter + 100-item preflight).** **2026-06-08 DeepSeek feasibility (user-driven, `a`): key VERIFIED working (cheap temp=0 spike `tools/spike_deepseek_feasibility.py`); descriptor RE-CALIBRATED from 558 real SSB records (`tools/analyze_descriptor_calibration.py`: op_density low/clustered p95=0.206, library saturated openpyxl=1.0) → op_density p95-normalized (`_OPDENS_REF=0.20`) + `_LINES_REF=117` → real-grid occupancy 8/16 → 16/16 (ADR-0006 addendum + `test_descriptor_spreads_real_fixtures_across_grid` ≥12/16 guard). A toy-task spike collapse was an ARTIFACT (real programs are bigger and spread). master `bbb57bc`, 39 passed. ⚠️ DeepSeek key pasted in chat → user advised to rotate it.** **S10 (root pyproject) + S11 (fork openai-compat commit `05a023c`) done; S15 model adapter `qd/adapter_skillopt.py` BUILT + proven end-to-end on real DeepSeek+SpreadsheetBench** (2-task smoke `tools/preflight_deepseek_smoke.py`: real rollout/grade/reflect/apply/gate; probe reads `predictions/<id>/code.py` → real descriptor cells 13/14; K=1 gate ties-reject; master `79ca77e`). ⚠️ target non-deterministic (temp>0) → needs frozen config (temp=0+seed, P2) before the real run. data_root = `SkillOpt/data/spreadsheetbench_verified_400` (task xlsx), items = `spreadsheetbench_split/test/items.json` (280). **Stage-4 validation RAN (master `a13c9b5`, ~$0.12, `tools/run_qd_validation.py`): K=1-vs-K=4 on 12 SSB tasks/DeepSeek, frozen target (temp=0+seed) + diverse optimizer (temp=0.8) → [Q1] QD EXPLORES (n_occupied=2) TRUE; [Q2] payoff FALSE/inconclusive (K=1 0.5 > K=4 0.417, CONFOUNDED by loop early-stop = unequal evals 4-vs-2, thin cell-clustered exploration, tiny budget). Real-run fixes: loop grid=16 (not arm-K); fork role-aware temp `0948d2d`.** Feasibility PROVEN end-to-end. **FAIR re-run DONE (master `c259b8f`, N=20/eval_budget=12, ~$0.7, loop now stall-tolerant so both arms spend full budget): K=1 greedy best=0.65 (9 evals) vs K=4 QD best=0.50 (12 evals, n_occupied=2, cross_cell=2) → [Q2] payoff FALSE — greedy BEATS QD at equal budget AND with fewer evals. HONEST NEGATIVE: QD-over-Skills does NOT pay off at this scale/benchmark/model (thin 2-cell exploration, greedy not stuck in a local optimum, short budget); core bet (QD>greedy) UNVALIDATED, small evidence against it.** To salvage QD: richer variation (skill edits that actually change behavior → thicker exploration = the real lever) / bigger budget (long search where greedy plateaus at 0.65) / a benchmark with deeper local optima. Next = DECIDE (salvage vs honestly conclude QD doesn't help here). **FULL-RUN LAUNCH SURFACE BUILT 2026-06-08: `scripts/run_experiment.py` (`--full` N=280 / `--preflight` 2-task / `--dry-run` zero-cost self-check; reads `.env` = the ONLY API-swap surface via `configure_deepseek`; frozen target temp0+seed42; writes `runs/<mode>/summary.json`, key-redacted) + `scripts/make_bundle.py` (self-contained air-gap zip + sha256; EXCLUDES `.env`/`.git`/fork `outputs`256M/non-SSB data) + `LOCAL_ONLY.md` (company cold-start SSOT: what the GitHub clone lacks + the unzip→.env→`--full` flow) + `qd/tests/test_company_launch.py` (8 zero-API guards incl. `.env`-never-in-zip). README/RUNBOOK reconciled — RUNBOOK previously pointed at VAPOR (`--config configs/*.yaml`, `verify_checkout.py`, `data/benchmarks/` — none existed). 47 passed; air-gap zip built `dist/skillopt-fullrun-b03dc28.zip` (18MB) sha256 `25d3b4aef3ff7628b7f1c54667dad3e24e9ddd97a3591cd43996cb7d7ae88870`. Company flow now = unzip → `pip install -e ./SkillOpt -r requirements-extra.txt` → edit `.env` → `python scripts/run_experiment.py --full`. **Committed master `a6075ea` + pushed (latest); delivery channel = GitHub Release `fullrun-v4` (latest, ONLY release kept — v1/v2/v3 deleted; commit master `15f1de3`)** (asset `skillopt-fullrun-15f1de3.zip` 18MB, sha256 `38f9bf7dbc55e65c0a281e39587ca0192ba6044308f4c5fc0af4393626e062cb`; **+`--probe-descriptor`** = cheap (~8-item, ~$0.2) pre-full check that descriptor resolves a given model's code, <3 cells ⇒ collapse ⇒ STOP (the Qwen3 morning-run failure mode); full run red-flags `n_occupied==1`; 52 tests; **`--full` eval_budget=24** (raised 12→24 per review, ~$20 DeepSeek); **`--tag <api>` → `runs/<mode>-<tag>/` for multi-API compare** (path-validated); **single canonical kickoff prompt `handoff/COMPANY_PROMPT.md`** — evergreen (points at LATEST release + sha-from-notes, never needs per-release edits), step-by-step download→ask-operator-for-API→.env→dry-run→preflight→full; operator MANUALLY sends it to the company agent (NOT auto-delivered — agent needs the prompt before it has the zip); N=280 = full SpreadsheetBench-Verified-400 TEST split (80 train/40 val/**280 test** = 400; 280 is the held-out eval, same set as the morning baseline — NOT "too few"); 51 zero-API tests; company DOWNLOADS from Releases / `gh release download` (latest=v3), NOT `git clone` — clone lacks the gitignored fork+data). Reviewed by code-reviewer + security-reviewer (no CRITICAL): security PASS (.env can't leak), hardened so `.env` is excluded by BASENAME at any depth + `.env.*` variants (was root-`.env`-only); `--full` now REQUIRES an explicit flag (no silent partial run); dry-run rejects an empty data dir; 49 zero-API tests. **COLD-PULL VERIFIED 2026-06-08: downloaded the release asset to a clean temp dir, fresh venv install (light deps, no torch/vllm), 49 tests + `--full` dry-run READY + a REAL `--preflight` produced summary.json from the extract = end-to-end runnable-from-scratch confirmed.** ⚠️ DeepSeek key STILL authenticated during that test → NOT yet rotated; user re-warned to ROTATE (re-exposed in chat multiple times). **CLEANUP 2026-06-09: deleted releases v1/v2 (only `fullrun-v3` remains) + deleted remote junk branch `傻逼代码`; its 1 commit `1f09f70` was NOT empty junk (DIVERGENT qd/loop.py +471 / archive+budget changes + `tools/preflight_spreadsheetbench.py` + `docs/EXPERIMENT_LOG_qd_full_20260608_114659.md` + `RUN_SSB.md` — likely the company's audited-divergent QD FULL run from 2026-06-08 11:46) → archived to LOCAL tag `archive/dead-branch-1f09f70` (recoverable via `git push origin archive/dead-branch-1f09f70:refs/heads/<name>`; not pushed). AUDITED 2026-06-09: that log is the company's morning FULL run on **Qwen3-235B-a22b-2507-fp8** (Tencent internal) — BROKEN: descriptor collapsed to 1/16 cell → QD did nothing → best == baseline 0.45, 0/8 accept, killed at test 43/280. NOT a usable QD-vs-greedy result (diagnostic only; confirms the audit). Two takeaways: (1) company tries MULTIPLE APIs (RUN_SSB.md documents OpenAI/Azure/DeepSeek; actual run used Qwen3) → "ask operator for API" design is right; (2) our descriptor was recalibrated on DeepSeek/real-SSB data NOT Qwen3 → real collapse risk on the company's model → **v4 `--probe-descriptor` is the cheap pre-full guard against exactly this**. Math doc/work-record on the branch are pre-reorg copies (redundant with `archive/pre-reorg-2026-06-08`). No code to merge.** **FULL-RUN RESULT 2026-06-09 (company, archived `docs/RESULTS-venus-qwen35.md`, commit `9b44ee6`): FIRST POSITIVE — `qwen3.5-397b-a17b` via venus llmproxy, full N=280 / equal 24-eval budget → K=4 QD best 0.600 (168/280) > K=1 greedy 0.579 (162/280) = +2.1pts / 6 tasks; n_occupied=2, cross_cell=1; Q1+Q2 BOTH hold. FLIPS the small-scale DeepSeek negative (greedy 0.65 > QD 0.50 @ N=20). v4 `--probe-descriptor` worked (8-task baseline → 6/16 cells, no collapse). CAVEATS (honest): single run, modest margin (6 tasks), thin exploration (2/6 cells), optimizer stochastic (temp=0.8, seed unpinned) → encouraging FIRST evidence for QD>greedy, NOT yet robust (needs multi-seed error bars / bigger budget or K). Authoritative `summary.json` is in the 321MB `returned-venus-qwen35.zip` (sha `013dec3859452cb938a449c6ee4278e791a3d5694b305e8fe3e81299504c4751`) — NOT in repo, needs durable storage. Internal venus endpoint redacted before the public commit.** **PAPER WRITING (2026-06-10): lives in `E:\skillopt\paper-local\` — LOCAL-ONLY, NEVER commit/push (public repo = scooping risk); isolated via `.git/info/exclude` entry `paper-local/` (NOT .gitignore, to leave zero public trace; even `git add -A` skips it, verified) → invisible to git, so future sessions must learn of it from THIS memory note. Contains `论文-方法论.md` = full Section 3 Methodology draft: operator-transfer story (DL operator toolkit → skill space; SkillOpt = minibatch-SGD baseline + K=1 exact-reduction guarantee; operators: QD archive w/ UCB+cross-cell+resolution probe, SkillAdam = edit-memory momentum + adaptive edit budget (Eq.5, principles-not-numerical-equivalence), SkillDropout = clause masking + load-bearing probe; budget-matched protocol w/ additive ladder + multi-seed paired stats). Writing rules (user-mandated, all paper files): follow `paper-submission-checklist` skill exactly (the .skill zip from WeChat; ≤25-word sentences, ≤1 clause, active voice, 总分 structure, no "based on"/"while", old-info-first, formulas explained immediately, fixed collocations), NO em-dashes (——/—), notation: scalar lowercase italic / vector lowercase bold / matrix uppercase NOT bold; experiment numbers stay OUT of methodology (live in Experiments, sourced from authoritative summary.json). User's framing was "碰瓷BP" hype; I steered to honest-analogy version (real ablatable mechanisms, analogy limits stated in-text) and user accepted. **v2 REWRITE 2026-06-10 (supersedes v1, same file): modeled on the BASE PAPER itself — `C:\Users\王奕豪\Downloads\2605.23904v2.pdf` = "SkillOpt: Executive Strategy for Self-Evolving Agent Skills" (Microsoft+SJTU, 27pp; full text extracted to `paper-local/skillopt-paper-fulltext.txt`; PDF reads fine via pypdf despite Read-tool 'password-protected' false alarm). THREE GOLD FINDS that reshape the story: (1) base paper says "The deep-learning analogy is operational rather than decorative", its §3.2/3.3 are literally titled "Forward Pass"/"Backward Pass: Minibatch Reflection", edit budget L_t = "the learning-rate analogue" → our paper = the SEQUEL completing the toolkit, not 碰瓷; (2) ⚠️ base paper ALREADY claims "epoch-wise slow/meta update acts like a momentum term" → SkillAdam MUST be positioned as momentum→Adam evolution (step-level first moment edit memory m_t + per-section second-moment budget split L_t^(j), Eq.5/6), explicitly differentiated from rejected-buffer/slow-meta, else reviewers kill it; (3) dropped "textual gradient" vocab (TextGrad is a baseline the base paper beats) → use their term "edit direction". v2 adopts base notation ((τ(s),r(s))=h(M,x,s), D_tr/D_sel/D_test, C(D_tr), L_t), extends their Fig.1 analogy dictionary as Table 1 with Status=inherited/this-work column, names the QD operator **SkillElites** (after MAP-Elites; naming awaiting user sign-off), and adds the nesting claim: every operator has an exact off-switch (k=1 / memory-off+uniform budget / p=0) so the framework nests SkillOpt like Adam nests SGD.** ⚠️ `--full` tests QD's CURRENT form only (propose = plain reflect; `qd/variation.py` aimed-sampling still NOT wired into `adapter.propose`). Still pending: S13/S14 vendoring (`vendor/SkillOpt`) + S16 heavy gate (verify_checkout / run_provenance / clean-tree).** Full plan + RESUME HERE in `REORG_PLAN.md`.

**RCV (2026-06-12, ADR-0007, commit `24ca1dd`): Reject-Conditioned Variation designed via brainstorm + CORE IMPLEMENTED.** Idea (user's): rejected edits already paid full expensive rollouts; their info (patch direction / Δscore / per-task flips from adapter rollout cache = FREE diff / descriptor cell) currently discarded → serialize as deterministic ledger, feed back to propose in-context (discrete-text pseudo-gradient; OPRO ascending-score + ProTeGi grounding, ours = ground-truth flips). Positioning (user-locked): INTO the QD paper as the「瞄准着采」implementation (3rd contribution; 3-arm ablation greedy/QD-plain/QD+RCV — first two arms = existing venus data); mechanism A raw-ledger core + B distill as flag + speculative pre-check as switch (4 guardrails: fail-open / never-skip-all-survivors / named-match-only contract / full skip logging); C stepping-stone reuse deferred. DONE zero-API: `qd/ledger.py` (LedgerEntry+RejectionLedger, render char-budgeted fold-oldest), `qd/loop.py` `run_search(use_ledger, pre_check)` (K=1 force-inert = red line; ledger kwarg only passed when non-None = back-compat; `ProposedCandidate.patch`), `tools/analyze_returned_stats.py` (exact McNemar + seeded paired bootstrap; **venus +6 tasks if disagreements 4↔10 → p≈0.18 = single run NOT significant**, written as test), 79 passed, code-reviewer APPROVE. **REMAINING RCV: adapter `extra_context` (AIM=verbalize target_cell per ADR-0006 axes + AVOID=ledger render + per-task flips diff) + fork `reflect(extra_context=None)` param (3rd fork commit) + `run_experiment.py --rcv/--rcv-precheck/--rcv-distill` third arm + T017 paid validation (T1 smoke $1 → T2 N=20 adversarial $5-10 → T3 full 3-arm DeepSeek $40-60 dual-model+flip-dissection → T4 multi-seed $100-150).** Strategy (user self-funds DeepSeek): company API = slow confirmation instrument (QPM-limited, 18.6h/run), DeepSeek = dev/ablation instrument (~$30/full-2-arm @ 59M tok, no QPM cap); dual-model evidence strengthens paper. **searchqa BOMBSHELL: company ran "our QD" on searchqa on a PRE-PROCESS junk branch (公司侧自扩, no provenance; searchqa rollouts have NO code.py → code-descriptor likely collapsed → n_occupied=1 → gain unattributable; board also says SearchQA near-ceiling = noise-prone). Audit request SENT: `handoff/RETURN_REQUEST-searchqa-audit.md` (P0 provenance/P1 n_occupied/P2 per-item/P3 trajectory samples for text-descriptor design) → verdict 抢救/重跑/判废 (T016 BLOCKED on company return).** ⚠️ venus 321MB `returned-venus-qwen35.zip` NOT on this machine (full C/D/E search) — T015 stats tool ready but data needs user to locate the zip. Board: T014 IN_PROGRESS (core done, adapter/fork next) / T015 IN_PROGRESS (tool done, data missing) / T016 BLOCKED / T017 TODO.

**VERIFIED (2026-06-08 deep read, for the loop+fix plan):**
- `qd/loop.py` **BUILT (S4, master `bd15c50`)** — `run_search` threads one shared `EvalCounter` + one frozen baseline + cosine `edit_budget` through both arms; `produce_and_score_candidate` reuses SkillOpt `rank_and_select` (lazy import) + gate-via-Archive; model injected via `CandidateProducer` (zero-API). Was MISSING at the 2026-06-08 deep read.
- **Descriptor grid collapse CONFIRMED empirically**: on the real fixtures both best+initial map to **cell=7** (nbins=4). axis1=1-uses_pandas saturates (0.9785/0.8423, both bin 3). The separation test only passes by asserting on raw float `b[1]`, not `.cell`. The gate's hint to "use iter_depth instead" does NOT work: mu[4]=iter_depth is **0.2194 on BOTH** fixtures (saturated + currently discarded by `project()`). axis1 must be rebuilt as a GRADED openpyxl/pandas API-call-mix ratio (or op-density ratio), not iter_depth. This requires amending/superseding **ADR-0002** (which currently sanctions 1-uses_pandas). **→ FIXED 2026-06-08 (S2/S3, ADR-0006): axis1 = op_density `n_ops/lines`; axis0 = (code_len+ctrl)/2; real non-degeneracy guard deferred to runtime `n_occupied>1`.**
- **K=1 test import is path-fragile, not broken-here**: `from skillopt...` works in the local `.venv` ONLY because of a per-machine **editable install** (`.venv/.../__editable__.skillopt-0.1.0.pth` → hardcodes `E:\skillopt\SkillOpt\skillopt`). `.venv` is gitignored, so a fresh checkout (the company) has no path → collection ERROR; and the reorg move `SkillOpt/`→`vendor/SkillOpt/` breaks the hardcoded `.pth`. Fix = root `pyproject.toml`/`conftest.py` pinning `vendor/SkillOpt` on sys.path + reinstall editable from the new location.
- **SkillOpt reuse-map line numbers all verified** against source: gate.py strict `cand_score > current_score` (123), `select_gate_score` (46); scheduler.py `round()` banker's rounding (71/82), stateful `_current_step` (34/42); clip.py truncation fallback `edits[:max_edits]` (108), no-op when `len<=max_edits` (54); trainer.py swap region (1316-1332), baseline-once `if current_score<0` (900-918); `select.py` is a dead re-export stub (import from `clip`). SkillOpt fork has only `upstream=microsoft` remote (no origin), HEAD `ee9931e`.

**COMPANY REPORT 2026-06-12 (EXPERIMENTS-OBSERVATIONS.md via WeChat) — SearchQA verdict CORRECTED + dual-benchmark story:** Company ran SearchQA FULL (N=1400 test, 24 evals/arm, venus qwen3.5, ~52h, proper process: probe gate + frozen config + official compute_score). **The "weak gain" the user heard was a MONITORING-CALIBER artifact (evalcurve drops TIMEOUTs from denominator → inflated 0.809 vs official 0.7907); OFFICIAL verdict: Q2=FALSE on SearchQA — K=4 QD 0.7907 ≈ K=1 0.7921 (tie, -2/1400 tasks). Q1 still True (n_occ=2, cross_cell=1).** SearchQA needed a NEW text-level descriptor (verbosity × answer length; first collapsed 2/16, recalibrated to 3/16 pass) = company-side code extension AGAIN (divergent code; audit package run_params.txt has correction log; split = HF-sampled non-official ids → not comparable to published numbers, arm-internal only). Evidence matrix now 2×2: SSB/qwen35 +2.14 ✓, SSB/deepseek +1.79 ✓ (our overnight dpsk-rcv-full), SearchQA/qwen35 tie ✗, SearchQA/deepseek not run. **Paper story sharpened: exploration = necessary-not-sufficient; QD pays where headroom+behavioral-richness exist (SSB both models), ties where baseline strong+behavior thin (SearchQA) — boundary-conditions narrative (report §5) better than win-everywhere. Old board decision "SearchQA 不作主战场(饱和)" empirically vindicated. RCV negative (explores 2x, best down) fits same frame (budget dilution).** T016 partially resolved by this report; still want audit zip (per-item stats + code delta of their searchqa adapter/descriptor to merge properly).

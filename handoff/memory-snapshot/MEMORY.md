# Memory Index

- [SkillOpt repro project](skillopt-repro-project.md) — E:\skillopt extends microsoft/SkillOpt via Quality-Diversity; authority = QD-over-Skills package (GitHub Yihao-DD/skillopt-repro; **master cleaned+pushed @ a975299 2026-06-08** = canonical clean state; worked by Claude local + Codex on AutoDL). **REORG in progress (S9/S18): repo cleaned (legacy DELETED, recoverable via `archive/pre-reorg-2026-06-08` tag); done S1 bugs + S2/S3 + real-data calibration descriptor (op_density **p95-normalized** `_OPDENS_REF=0.20`, `_LINES_REF=117`, ADR-0006; 558 real SSB records occupy **16/16** grid, was 8/16) + S4–S9 `qd/loop.py` (two-arm; one shared EvalCounter = equal budget; K=1==gate/F3; K>1 routing + dedup + UCB parent-cell + cross-cell; **39 passed**, zero-API; master `bbb57bc`); **DeepSeek key VERIFIED**; S10 (root pyproject) + S11 (fork fix `05a023c`) done; **S15 adapter `qd/adapter_skillopt.py` BUILT + proven end-to-end on real DeepSeek+SpreadsheetBench** (2-task smoke `tools/preflight_deepseek_smoke.py`: real rollout/grade/reflect/apply/gate; probe reads `predictions/<id>/code.py` → real cells 13/14; K=1 gate ties-reject; master `79ca77e`). ⚠️ target non-deterministic (temp>0) → needs frozen config (temp=0+seed) before the real run. **Stage-4 FAIR validation DONE (master `c259b8f`, N=20/eval_budget=12, ~$0.7, `tools/run_qd_validation.py`): baseline 0.45; K=1 greedy best=0.65 (9 evals — optimizer exhausted distinct candidates); K=4 QD best=0.50 (12 evals, n_occupied=2, cross_cell=2). [Q1] QD EXPLORES TRUE; [Q2] payoff FALSE — greedy K=1 BEATS QD K=4 (0.65 vs 0.50) at equal budget AND with fewer evals. HONEST NEGATIVE RESULT: at this scale/benchmark/model QD-over-Skills does NOT pay off (thin exploration = 2 cells, greedy not stuck in a local optimum, short budget). Core bet (QD>greedy) UNVALIDATED; small evidence against it.** Feasibility (the loop runs + explores on real DeepSeek) PROVEN; payoff refuted at this scale. Fixes en route: loop grid=16, fork role-aware temp `0948d2d`, loop stall-tolerant budget. To salvage QD: richer variation (thicker exploration — the real lever) / bigger budget (long search where greedy plateaus) / a benchmark with deeper local optima. next = DECIDE (salvage vs honestly conclude QD doesn't help here); S13/S14 packaging + S16 gate still pending. ⚠️ DeepSeek key pasted in chat 2026-06-08 → advise rotation.** T000/T001/T002 DONE; SpreadsheetBench baseline EM +7.1pts (greedy plateau reproduced); critical path BLOCKED on BLOCKER-1 (company API/budget).
- [E:\skillopt write permission](skillopt-dir-write-permission.md) — writes need a one-time elevated `icacls` grant (process runs non-elevated); not a sandbox issue.
- [Overnight mandate 2026-06-12](overnight-mandate-2026-06-12.md) — T2 后自主判定烧全量，11:00 交结果；没跑成则备好优化方案
- [Faithful-repro lesson 2026-06-13](faithful-repro-lesson.md) — **最新权威状态**：对照基线必须忠于原文不能阉割（之前 QD 赢对原文不可信，greedy 缺 slow/meta+buffer）；B 方案重构中（三集分离+slow done，缺口3 buffer/缺口2 meta 待做）；入口 `docs/FAITHFUL-REPRO-plan.md`

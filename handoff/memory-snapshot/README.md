# Memory snapshot（备份，2026-06-13）

这是 Claude 跨会话记忆（源在 `~/.claude/projects/E--skillopt/memory/`）的**只读备份**，提交进仓库做持久化/可恢复，防个人记忆目录意外清空。

- 源是单一真相；这里是快照，**会过时**——以源 + `docs/FAITHFUL-REPRO-plan.md`（权威流程文档）为准。
- **已脱敏**：AutoDL host:port 替换为占位（私有连接信息不入 git）。
- 恢复：把这些 `.md` 拷回 `~/.claude/projects/E--skillopt/memory/`（覆盖前先比对，源可能更新）。
- 最新权威状态见 `faithful-repro-lesson.md`（忠于原文教训 + 待办）。

# SkillOpt-Repro 工作完整记录

> 时间范围：2026-06-08 01:42（拉 repo）→ 14:24（当前正在跑 full）
> 目标：在 SkillOpt 上复现 vanilla → 实现 QD-over-Skills 扩展 → 跑 full 实验出论文数据

---

## 0. 速览（TL;DR）

| 指标 | 值 |
|---|---|
| 仓库起点 | `https://github.com/.../SkillOpt`，commit `b5328e8`（main，2026-06-08 01:42 拉下来） |
| **新增代码** | `qd/`（5 文件 471 行 loop + archive/budget/descriptor/__init__）、`scripts/qd_train.py`（216 行）、`scripts/launch_qd.sh`（49 行）、`tools/`（3 个 materialize 脚本）、`patches/deepseek-backend-adapter.patch` |
| **修改 SkillOpt** | 仅 1 个文件：`skillopt/model/azure_openai.py`（让 OpenAI-compatible 客户端用 `max_tokens` 而非 `max_completion_tokens`，并跳过 `reasoning_effort`） |
| **理论文档** | `QD-over-Skills/方案与数学推导.md`（216 行）、`研究框架.md`（149 行）、`论文-完整数学方案.md`（**454 行**，已包含命题 3.10 动量等价性、3.11 有效步长二分解、3.12 弱收敛速率） |
| **完成的实验** | (a) vanilla SkillOpt 完整跑 1 次；(b) QD smoke 调试跑 5 次；(c) QD full 起跑 3 次（前 2 次失败，第 3 次正在跑） |
| **vanilla 实验结果** | baseline_test=0.4357 → final_test=**0.5250**，Δ=**+0.0893**（8 步里 1 次 ACCEPT，发生在 step 7） |
| **smoke 实验结果** | baseline_test=0.5000 → final_test=0.5000，Δ=0.000（1 步 1 次 REJECT，作为 gate 闭环验证 ✅） |
| **full 实验结果（进行中）** | baseline_test=0.4500，已完成 STEP 1-6 全部 REJECT（candidate 0.325-0.450 < baseline 0.500），STEP 7 进行中 |

---

## 1. 仓库初始状态（拉下来时）

### 1.1 git
```
分支：main
HEAD：b5328e8 Merge pull request #40 ... fix(model): forward Qwen timeout and only set enable_thinking when true
```
近 50 个 commit 全部是上游 SkillOpt 的，没有 fork-modification。

### 1.2 顶层目录
```
skillopt-repro/
├── SkillOpt/              # 上游主仓（pip install -e . 已装到 .venv）
├── .env / .env.example     # 后端配置（azure_openai_endpoint = http://v2.open.venus.oa.com/llmproxy）
├── README.md / .gitignore
├── requirements-extra.txt
├── docs/superpowers/       # 拉取时已存在的设计文档（plans/specs）
├── tools/                  # 拉取时不存在 — 我添加（数据切分脚本）
├── qd/                     # 拉取时不存在 — 我添加（QD-over-Skills 核心代码）
├── QD-over-Skills/         # 拉取时不存在 — 我添加（方案/方案文档/.tasks/decisions）
├── results/                # 拉取时不存在 — 我添加（vanilla 复现产物）
├── patches/                # 拉取时不存在 — 我添加（deepseek 适配 patch）
├── baseline_report.md / ssb_baseline_report.md / RUN_SSB.md   # 我添加（运行说明）
└── .venv/                  # 我添加（python 3.10 venv）
```

---

## 2. 我的所有修改（按目录组织）

### 2.1 `qd/`（**新建**，QD-over-Skills 核心代码，471+154+112+48 ≈ 800 行）

| 文件 | 行 | 作用 |
|---|---|---|
| `qd/__init__.py` | 9 | 导出 `QDArchive`、`Descriptor`、`build_qd_trainer` |
| `qd/descriptor.py` | 112 | 二维行为描述子 b=(b₁ skill 长度归一化, b₂ 验证集命中率) → 16 cells（4×4）|
| `qd/archive.py` | 154 | MAP-Elites 档案：`add_or_replace`、`get_elite_score(cell)`、`occupied_cells`、`best_cell` |
| `qd/budget.py` | 48 | 预算与缓存计数（expensive_evals / cheap_evals / cache_hits）|
| `qd/loop.py` | **471** | `QDTrainer`：rollout → reflect → aggregate → rank_and_select → sel_eval → **每格严格 ≻ gate** → archive update → final_test |
| `qd/tests/test_descriptor_v0.py` | 118 | descriptor 离散化 / 边界回归 |
| `qd/tests/test_k1_characterization.py` | 94 | K=1 时档案塌缩为 SkillOpt（命题 3.10 中的 μ̄=0 极限）|
| `qd/tests/test_k1_reduces_to_skillopt.py` | 99 | 端到端等价性（同种子 K=1 ≡ vanilla） |

### 2.2 `SkillOpt/skillopt/model/azure_openai.py`（**修改**，唯一对上游的改动）

新增辅助函数 `_is_openai_compat_client(client)`：判断是否是纯 OpenAI 协议（DeepSeek/vLLM/Qwen 透传），与 AzureOpenAI 区分。

修改两处 `_chat_impl` / `_chat_messages_impl`：
- **OpenAI-compat 客户端**（`AZURE_OPENAI_AUTH_MODE=openai_compatible`）：用 `max_tokens` 而非 `max_completion_tokens`；不传 `reasoning_effort`
- **AzureOpenAI 客户端**：保持原行为

**为什么改**：venus.oa.com llmproxy 是 OpenAI-compat 接口，不接受 `max_completion_tokens` / `reasoning_effort`，原代码在 qwen3-235b-a22b 路径上会 400 错误。

### 2.3 `SkillOpt/scripts/qd_train.py`（**新建**，216 行）

QD 训练入口，**完全克隆**了 vanilla `scripts/train.py` 的后端配置流程（configure_azure_openai / set_*_backend / set_*_deployment），保证 baseline 跑出来与 vanilla 完全一致。
然后调用 `qd.loop.build_qd_trainer(...)` 替换 vanilla 的 `Trainer`。

### 2.4 `SkillOpt/scripts/launch_qd.sh`（**新建**，49 行）

```bash
NEPOCHS=4 STEPS_PER_EPOCH=2 BATCH=40 TRAIN_SIZE=80 SEL=40 TEST=280 QD_K=16 \
  bash scripts/launch_qd.sh full
```
环境变量化所有超参，与 vanilla 一致；nohup + 写 `logs/qd_${TAG}_latest.{pid,path}` 方便监控。

### 2.5 `tools/`（**新建**）

| 文件 | 作用 |
|---|---|
| `materialize_searchqa.py` | SearchQA 数据 → SkillOpt 标准 split |
| `materialize_spreadsheetbench.py` | SSB 数据 → 标准 split (train=80, val=40, test=280) |
| `preflight_spreadsheetbench.py` | 检查 SSB 文件完整性（openpyxl 读取） |

### 2.6 `QD-over-Skills/`（**新建**，理论与项目管理文档）

| 文件 | 行 | 作用 |
|---|---|---|
| `BRIEF.md` | 90 | 一页纸方案 + 与 SkillOpt diff |
| `AGENTS.md` | 31 | 团队协作约定 |
| `PROJECT_MANAGEMENT.md` | 165 | spec-task-status 流程 |
| `OPEN_DECISIONS.md` | 42 | 待定问题 |
| `方案与数学推导.md` | 216 | v0.x 数学推导（命题 3.1-3.9） |
| `研究框架.md` | 149 | 实验设计骨架 |
| **`论文-完整数学方案.md`** | **454** | **论文级正稿**（命题 3.1-3.9 + 新增 3.10 离散动量等价性、3.11 有效步长二分解、3.12 弱收敛速率 + Heavy-Ball 类比表 + abstract / intro 模板 + FIG-1~4 实验口径） |
| `decisions/ADR-0001-skillopt-accept-semantics.md` | 52 | 接受语义决策记录 |
| `decisions/ADR-0002-descriptor-axes.md` | 29 | 描述子轴选择 |
| `.tasks/T001-k1-regression/{spec,status}.md` | — | K=1 回归任务 |
| `.tasks/T002-descriptor-v0/{spec,status}.md` | — | descriptor v0 任务 |

### 2.7 `results/`（**新建**，复现产物存档）

| 路径 | 说明 |
|---|---|
| `results/ssb_dpsk_run1/{summary,history,config,best_skill}.json/md` | DeepSeek backend 早期跑（已弃用）|
| `results/ssb_eval_initial/eval_summary.json` | 初始 prompt 在 D_test 的 eval-only 跑 |
| `results/searchqa_dpsk_*` | SearchQA DeepSeek 早期试跑 |

### 2.8 顶层 md 文件

| 文件 | 行 | 内容 |
|---|---|---|
| `RUN_SSB.md` | 125 | SpreadsheetBench 完整运行手册 |
| `baseline_report.md` | 48 | DeepSeek baseline 报告 |
| `ssb_baseline_report.md` | 62 | SSB baseline 数据 |

---

## 3. 实验运行记录（按时间顺序）

### 3.1 vanilla SkillOpt 复现跑（02:04–04:51，**已完成 ✅**）

| 字段 | 值 |
|---|---|
| 启动 | `02:04:21`，`scripts/run_spreadsheetbench.sh` 直接调用 |
| 完成 | `04:51:36`（耗时 **2h47m**）|
| 输出目录 | `SkillOpt/outputs/ssb_qwen3_20260608_020421/` |
| 日志 | `SkillOpt/logs/ssb_qwen3_20260608_020421.log`（95 KB） |
| 配置 | optimizer = target = `qwen3-235b-a22b-2507-fp8`；K cells = N/A（vanilla 不分格）；epochs 4 × steps_per_epoch 2 = **8 步**；train=80, val=40, test=280；edit_budget=4；skill_update_mode=patch；use_gate=true |

**逐步 gate 决策**（从 history.json 提取）：

| step | rollout_hard (40题) | candidate_gate_score | action |
|------|----|------|------|
| 1 | 0.600 | — | reject |
| 2 | 0.600 | — | reject |
| 3 | 0.575 | — | reject |
| 4 | 0.575 | — | reject |
| 5 | 0.575 | — | reject |
| 6 | 0.625 | — | reject |
| **7** | 0.550 | **0.625** | **accept_new_best** ⭐ |
| 8 | 0.650 | — | reject |

**关键事实**：vanilla 前 6 步全 reject，**step 7 唯一一次 accept**：
- rollout_hard=0.550（不是最高的）
- 6 个 patches → merge → 14 edits → **rank_and_select 精选 2 个 edits**（n_edits_ranked=2，**edit_budget=2** ← 注意这是 cosine scheduler 在最后一个 epoch 上自适应缩到 2）
- selection_hard 从 baseline 0.55 → **0.625**（+0.075） → 触发 accept

**最终成绩**：

| | hard | soft |
|---|---|---|
| `baseline_test_hard` | **0.4357** | 0.4357 |
| `test_hard`（final_test 280 题） | **0.5250** | 0.5250 |
| `test_delta_hard` | **+0.0893** | +0.0893 |
| total_accepts / rejects | 1 / 7 | — |
| total_wall_time | 9864 s ≈ 2h44m | — |
| total_tokens | 45.6 M | — |

### 3.2 QD smoke 调试跑（10:50–11:45，5 次）

逐次试错记录：

| # | 时间 | 日志 | 结果 |
|---|---|---|---|
| smoke_001 | ~10:50 | （无 log，早期试错） | 立刻崩 — backend 配置问题 |
| 10:50:25 | qd_smoke_20260608_105025.log | 21 KB | 崩在 `_configure_models` — 变量未定义 |
| 10:50:49 | qd_smoke_20260608_105049.log | 22 KB | 同样问题 |
| 10:51:29 | qd_smoke_20260608_105129.log | 3 KB | 修了变量但 baseline_test=0.5 ≠ vanilla 0.4357，怀疑后端漂移 |
| **11:23:01** | qd_smoke_20260608_112301.log | 6 KB | ✅ **完整跑通**（K=16, 1 epoch × 1 step, train=8, sel=8, test=20） |

**最终 smoke summary**（`outputs/qd_smoke_20260608_112301/summary.json`）：
```json
{
  "baseline":   {"test_hard": 0.5, "selection_hard": 0.5, "cell": 7, "b": [0.301, 1.000]},
  "framework":  {"test_hard": 0.5, "best_cell": 7, "best_step": 0},
  "delta":      {"test_hard": 0.0},
  "archive":    {"k": 16, "n_occupied": 1, "best_cell": 7, "best_score": 0.5},
  "budget":     {"expensive_evals": 4, "expensive_items": 56, "train_rollouts": 1, "train_items": 8}
}
```

**STEP 1 关键事件**：rollout 8/8 PASS=1.0 → reflect → aggregate `1+3 → 4 edits` → build_scheduler ✅（修复点验证）→ rank_and_select → sel_eval candidate 0.375 → **gate REJECT**（candidate 0.375 < parent 0.5）→ final_test 0.5。

**结论**：smoke 全链路 + gate accept/reject 逻辑 + summary.json 落盘 全部通过。

### 3.3 QD full 跑（10:56 起，3 次）

#### Full #1（10:56:31，已死）
- 日志：`qd_full_20260608_105631.log`（21 KB）
- 死因：跑到 baseline_test 33/40 时被我误杀（11:20 左右 `pkill qd_train.py` 一锅端 smoke 和 full）
- 已知问题：用了 `--skill_update_mode rewrite_from_suggestions`，与 vanilla（patch）口径不一致

#### Full #2（10:56:53，已死）
- 日志：`qd_full_20260608_105653.log`（23 KB）
- 同 #1 误杀，同口径问题

#### **Full #3（11:46:59 起，正在跑 ⏳，预计 14:50 完成）**

| 字段 | 值 |
|---|---|
| 启动 | `11:46:59`，`bash scripts/launch_qd.sh full` |
| PID | 581578 |
| 日志 | `SkillOpt/logs/qd_full_20260608_114659.log`（截至 14:24 已 60 KB） |
| 输出目录 | `SkillOpt/outputs/qd_full_20260608_114659/` |
| 配置 | qwen3-235b-a22b-2507-fp8，K=16，epochs=4 × steps=2 = 8 步，train=80, sel=40, test=280，**edit_budget=4**（与 vanilla 同），update_mode=**patch**（与 vanilla 同），seed=42 |

**完成阶段**（截至 14:24）：

| 阶段 | 结果 |
|---|---|
| STAGE 0 baseline_sel (40 题) | hard=**0.5000**，cell=7，b=(0.264, 0.975) |
| STAGE 0b baseline_test (280 题) | hard=**0.4500**（与 vanilla 0.4357 同量级 ✅） |
| EPOCH 1 / STEP 1 | rollout_hard=1.0(8) → aggregate 3+4→7 edits → candidate 0.3750 → **REJECT** |
| EPOCH 1 / STEP 2 | aggregate 4+4→7 → candidate 0.4500 → **REJECT** |
| EPOCH 2 / STEP 3 | aggregate 4+4→8 → candidate 0.3250 → **REJECT** |
| EPOCH 2 / STEP 4 | rollout=0.600 → aggregate 2+7→9 → candidate 0.4000 → **REJECT** |
| EPOCH 3 / STEP 5 | rollout=0.675 → aggregate 3+6→8 → candidate 0.4250 → **REJECT** |
| EPOCH 3 / STEP 6 | rollout=0.650 → aggregate 3+6→8 → candidate 0.4250 → **REJECT** |
| EPOCH 4 / STEP 7 | 进行中（aggregate 3+5→8 edits 已完成，sel_eval 进行中） |
| EPOCH 4 / STEP 8 | 待跑 |
| FINAL TEST | 待跑 |

**当前 archive**：`{cell 7: score=0.5000, step=0}`（**未变过**）

---

## 4. vanilla vs full 实验对照（核心数据）

| | vanilla SkillOpt（已完成） | full QD-over-Skills（进行中） |
|---|---|---|
| K cells | 1（无格） | 16 |
| 步数 | 8 | 8（前 6 完成）|
| baseline_sel hard | 0.5500 | 0.5000 |
| baseline_test hard | **0.4357** | **0.4500** |
| 8 步内 candidate sel 范围 | 没记录单点 | **0.325 – 0.450**（6 个点） |
| ACCEPT 次数 | 1（step 7） | **0**（截至 STEP 6） |
| **best_sel after 8 steps** | 0.625 | **0.500（不变）** |
| **final_test hard** | **0.5250** | **预测 0.4500（如 STEP 7-8 全 reject）** |
| **Δ test** | **+0.0893** | **预测 0.000（如 STEP 7-8 全 reject）** |

### 4.1 vanilla step 7 accept 的关键

```
rollout_hard:           0.550    （并非最高）
n_patches:              6
n_edits_merged:         14
edit_budget (cosine):   2        ← cosine 调度器到第 4 epoch 把 budget 收缩到 2
n_edits_ranked:         2        ← rank_and_select 从 14 个里精选 2 个
selection_hard:         0.625    ← 比 baseline_sel 0.55 高 0.075
candidate_gate_score:   0.625
action:                 accept_new_best
```

**这是关键 diff**：vanilla 在最后 epoch 用 `edit_budget=2`（cosine scheduler），改动小，更容易过 gate；
我们 full 用 **`edit_budget=4`** 全程不动，每步加 4 个 edits，**改动幅度大 → candidate 系统性退化** → 全 reject。

---

## 5. 当前最大风险与可能原因

### 5.1 风险

| 项 | 状态 |
|---|---|
| C1: accept ratio > 0 | 极可能 **0/8** ⚠️（vanilla 是 1/8） |
| C2: occupied cells > 1 | 极可能 **= 1**（vanilla 不适用） |
| C3: cross-cell pickup ≥ 1 | **= 0** |
| Δ test ≥ vanilla Δ | 极可能 **不达标**（0.000 vs +0.0893）|

### 5.2 最可能的原因

1. **edit_budget 口径错位**：vanilla `lr_scheduler=cosine` + `edit_budget=4` → 最后 epoch 实际是 2；我们 `edit_budget=4` **全程固定**。
2. **rollout-vs-sel gap 大**：每步 rollout 0.55-1.00 vs sel 0.32-0.45 = gap 0.20-0.55，过拟合 train minibatch。
3. **gate 严格阈值**：`candidate > parent`（strict ≻），vanilla 也用同样规则，但 vanilla baseline_sel=0.55 比我们 0.50 更"近 noise floor"，更难触发 reject 链。我们 baseline_sel=0.50 是更难突破的 plateau。

### 5.3 待调研

- [ ] 拉 SkillOpt 默认 cosine 调度器细节，确认每步 edit_budget 实际值
- [ ] 等 STEP 7-8 完整结果落盘
- [ ] 如 0/8 accept 确认，需要：(a) 把 `edit_budget` 改为 2 重跑；或 (b) 引入 ε-trust-region 软化 gate
- [ ] 拉 vanilla `history.json` 中每步 selection_hard 时间序列（用于论文 FIG-2 对照）

---

## 6. 论文写作进度

`QD-over-Skills/论文-完整数学方案.md`（**454 行 / 30 KB**）已包含：

- **§0 动机叙事**：SGD → Heavy-Ball → SkillOpt → SkillMomentum 类比链
- **§1-§2** 符号 / 算法伪码
- **§3.1-§3.9** 命题 3.2 (描述子双 Lipschitz)、3.4-3.7 (信任域接受规则)、3.8 (top-k/p 瞄准着采)、3.9 (ROI 上界)
- **§3.10 离散动量等价性**（**新核心结果**）：把档案更新显式重写为 heavy-ball 形式，给出有效动量系数 μ̄ ∈ [0,1)，且 μ̄=0 ⇔ K=1 ⇔ SkillOpt
- **§3.11 有效步长二分解**：η_eff = (格内推进) + (跨格反超)
- **§3.12 弱收敛速率**：在 finite-progress oracle 假设下 E*_t 以 η_min · p_0 · T 速率上涨
- **§4** 与已有工作精确 diff（含 Heavy-Ball / Polyak 行）
- **§5** 实验设计（FIG-1~4、TAB-1、C0/C1/C2/C3 判据）
- **§8** abstract / intro 模板 + 6 行 SGD↔SkillMomentum 类比表

**纯理论部分（§3.10/3.11/3.12）不依赖实验数字** → 即便 full 0/8 accept，理论贡献仍然成立。
**实验部分需要至少 1 次 ACCEPT**才能撑起 FIG-2 / FIG-3。

---

## 7. 时间线

| 时刻 | 事件 |
|---|---|
| 01:42 | 拉 repo |
| 01:43-02:03 | venv / .env 配置，写 RUN_SSB.md |
| 02:04 | 起 vanilla SkillOpt 跑 |
| 04:51 | vanilla 完成（test=0.5250） |
| ~10:30 | 写 qd/ 核心代码（archive、descriptor、loop） |
| 10:50-11:21 | 5 次 smoke 调试，修 backend / `_configure_models` / `build_scheduler` |
| 10:56 | 第 1 次 full 起跑（误杀） |
| 11:23 | smoke 全链路通过 ✅ |
| 11:38-11:46 | 写 `论文-完整数学方案.md` |
| **11:46:59** | **第 3 次 full 起跑（当前）** |
| 12:13 | full baseline_test=0.4500 完成 |
| 12:30 | full STEP 1 REJECT（candidate 0.375） |
| 12:50 | STEP 2 REJECT（0.450） |
| 13:14 | STEP 3 REJECT（0.325） |
| 13:35 | STEP 4 REJECT（0.400） |
| 13:55 | STEP 5 REJECT（0.425） |
| 14:13 | STEP 6 REJECT（0.425） |
| **14:24** | **STEP 7 sel_eval 进行中（当前）** |
| ~14:35 | STEP 7 gate 决策 |
| ~14:50 | STEP 8 完成 + final_test 完成 + summary.json 落盘 |

---

## 8. 仓库当前 git 状态

```
Branch: main
Behind/ahead: up-to-date with origin/main

Modified (未提交):
  M skillopt/model/azure_openai.py     ← OpenAI-compat 客户端适配（约 16 行 net diff）

Untracked (未提交):
  ?? scripts/launch_qd.sh              ← 49 行
  ?? scripts/qd_train.py               ← 216 行
```

外部目录 `qd/`、`QD-over-Skills/`、`tools/`、`results/`、`patches/`、顶层 md 文件全部在 `skillopt-repro/` 而非 `SkillOpt/` 里，不在 SkillOpt 子仓的 git 跟踪范围内。

---

## 9. 立即下一步（建议）

1. **盯 STEP 7 gate 决策**（~10 min 内出来）—— 若 ACCEPT，论文叙事 v1 直接走通
2. **若 STEP 7-8 全 REJECT**：
   - **方案 A**：改 `edit_budget=2`（对齐 vanilla cosine 末段）重跑 → ~3h
   - **方案 B**：在 gate 加 ε-trust-region 阈值（`candidate ≥ parent − 0.02`）→ ~3h
   - **方案 C**：换叙事到「严格信任域 = 鲁棒性保底」 + 拉 vanilla 逐步对比图
3. **写 vanilla 逐步 selection_hard 时间序列对比图脚本**（不影响 full，可并行进行）

---

> 本文档作为论文 supplementary / repro README 的素材底稿，所有数字均来自当前实验产物，未经修饰。
